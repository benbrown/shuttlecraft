<!DOCTYPE html>
<html>
  <head>
    <title>{{me.preferredUsername}}'s shuttlecraft</title>
    <link rel="stylesheet" href="{{rootUrl}}/css/secret.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1">
    <meta name="theme-color" content="#111122" />
    <link rel="apple-touch-icon" href="/images/apple-touch-icon.png"/>
    <script src="/app.js"></script>
  </head>
  <body>
        <div id="wrapper">
            <div id="nav">
                <nav>
                    <ul>
                      <li id="logo"><a href="{{rootUrl}}/private">{{prefs.icons.mascot}}</a></li>
                      <li {{#isEq url "/"}}class="current"{{/isEq}}><a href="{{rootUrl}}/private" {{#isEq url "/"}}onclick="return app.showMenu();"{{/isEq}}>{{prefs.icons.latest}}</a><span class="badge newPostsBadge" hidden></span></li>
                      <li {{#isEq url "/notifications"}}class="current"{{/isEq}}><a href="{{rootUrl}}/private/notifications">{{prefs.icons.notifications}}</a><span class="badge newNotificationsBadge" hidden></span></li>
                      <li {{#isEq url "/dms"}}class="current"{{/isEq}}><a href="{{rootUrl}}/private/dms">{{prefs.icons.messages}}</a><span class="badge newDMsBadge" hidden></span></li>
                      <li {{#isEq url "/post"}}class="current"{{/isEq}}><a href={{rootUrl}}"/private/post">{{prefs.icons.post}}</a></li>
                    </ul>
                </nav>
            </div>
            <div id="menu" class="{{#if nonav}}skinny{{/if}}">
                <nav>
                    <ul id="top_nav">
                        <li {{#isEq url "/"}}class="current"{{/isEq}}><a href="/private" title="Latest posts">{{prefs.icons.latest}}<span> Latest</span></a><span class="badge newPostsBadge" hidden></span></li>
                        <li {{#isEq url "/notifications"}}class="current"{{/isEq}}><a href="/private/notifications" title="Notifications">{{prefs.icons.notifications}}<span> Notifications</span></a><span class="badge newNotificationsBadge" hidden></span></li>
                        <li {{#isEq url "/dms"}}{{#unless ../feed}}class="current"{{/unless}}{{/isEq}}><a href="/private/dms" title="Private message inbox">{{prefs.icons.messages}}<span> Messages</span></a><span class="badge newDMsBadge" hidden></span></li>
                        <li {{#isEq url "/post"}}class="current"{{/isEq}}><a href="/private/post" title="Post something new">{{prefs.icons.post}}<span> Compose</span></a></li>
                        <li {{#isEq feed.id me.id}}class="current"{{/isEq}}><a href="/private/feeds/{{getUsername me.id}}" title="View my posts">{{prefs.icons.myPosts}}<span> My Posts</span></a></li>
                        <li {{#isEq url "/prefs"}}class="current"{{/isEq}}><a href="/private/prefs" title="Preferences">{{prefs.icons.prefs}}<span> Prefs</span></a></li>
                        <li><a href="https://github.com/benbrown/shuttlecraft/issues" title="Report a bug"><span>Bugs & Issues </span>🐛</a></li>
                    </ul>

                    <button onclick="app.showMenu()" id="close_menu">❌</button>
                    <ul id="top_feeds">
                        <li>
                          <form method="get" action="/private/find">
                              <label for="lookup">find a person</label>
                              <input type="search" placeholder="user@instance.town" id="lookup" name="handle" />
                              <div id="lookup_results"></div>
                          </form>
                        </li>
                        {{> feeds feeds=feeds currentFeed=feed dms=nonav}}
                        {{#unless expandfeeds}}
                        <li><a href="/private/following" title="Next 100" onclick="return app.loadMoreFeeds();">...</a></li></ul>
                        {{/unless}}
                </nav>
            </div>
            <div id="content" class="{{#if nonav}}nonav{{/if}}">
            {{{body}}}
           </div>
        </div>
    </body>
</html>
