<div class="byline">
    <a href="{{actor.id}}"><img src="{{actor.icon.url}}" class="avatar"></a>
    <div>
        <a href="{{actor.id}}" class="author">{{actor.name}}</a>
        <span class="handle">{{getUsername actor.id}}</span>
    </div>
    <div>
        {{#if actor.isFollowing}}{{else}}
        <button class="follow" onclick='return app.toggleFollow(this, "{{actor.id}}")'>
            <span class="active">😎</span>
            <span class="inactive">🫥</span>
        </button>
        {{/if}}
    </div>
</div>